Build the war with Maven
Build the Docker image from the dockerfile with "docker build --tag=ping ." 

Run the Docker image with "docker run -d -p 8080:8080 --name pingtest ping"


see also http://www.adam-bien.com/roller/abien/entry/java_ee_and_docker_quickstart